rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Symptom images - anyone can read, only admins can write
    match /symptom-images/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
  }
}
